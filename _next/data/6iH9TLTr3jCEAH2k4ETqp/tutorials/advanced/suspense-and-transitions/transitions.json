{"pageProps":{"siteName":"React JS Quiz","google_analytics_tracking_id":"G-ZW8BQT2FR5","google_adsense_publisher_id":"pub-7544108447932228","tutorial":{"id":14,"slug":"suspense-and-transitions","level":"advanced","order":2,"title":"Suspense and Transitions","seoTitle":"React Suspense and Transitions Tutorial","description":"Declare loading states with Suspense, keep the interface responsive with transitions and deferred values, and lazy load components without jank.","estimatedMinutes":17,"topics":["suspense","transitions","usetransition","usedeferredvalue","lazy loading","lazy-loading","code splitting","code-splitting","useoptimistic","concurrent"],"publishedAt":"2026-08-20","updatedAt":"2026-08-20","path":"/tutorials/advanced/suspense-and-transitions","quizPath":"/tutorials/advanced/suspense-and-transitions/quiz","chapters":[{"slug":"suspense-basics","title":"Suspense Basics","description":"Declaring a fallback once instead of threading isLoading everywhere.","order":1,"path":"/tutorials/advanced/suspense-and-transitions/suspense-basics"},{"slug":"lazy-loading-components","title":"Lazy Loading Components","description":"Splitting the bundle with lazy and Suspense.","order":2,"path":"/tutorials/advanced/suspense-and-transitions/lazy-loading-components"},{"slug":"transitions","title":"Transitions","description":"Marking updates as non-urgent so typing never stutters.","order":3,"path":"/tutorials/advanced/suspense-and-transitions/transitions"},{"slug":"optimistic-updates","title":"Optimistic Updates","description":"Showing the result before the server confirms it.","order":4,"path":"/tutorials/advanced/suspense-and-transitions/optimistic-updates"}],"chapterCount":4,"exerciseCount":1,"quizQuestionCount":8,"quiz":{"questions":[]}},"chapter":{"slug":"transitions","title":"Transitions","description":"Marking updates as non-urgent so typing never stutters.","order":3,"path":"/tutorials/advanced/suspense-and-transitions/transitions","blocks":[{"type":"paragraph","text":"Some updates must feel instant - a keystroke appearing in an input. Others can wait a moment - the filtered results below it. `useTransition` lets you say which is which."},{"type":"code","code":"import { useState, useTransition } from \"react\";\n\nfunction SearchPage() {\n  const [query, setQuery] = useState(\"\");\n  const [results, setResults] = useState([]);\n  const [isPending, startTransition] = useTransition();\n\n  function handleChange(event) {\n    setQuery(event.target.value);          // urgent: show the typing\n\n    startTransition(() => {\n      setResults(search(event.target.value));   // can be interrupted\n    });\n  }\n\n  return (\n    <>\n      <input value={query} onChange={handleChange} />\n      <div style={{ opacity: isPending ? 0.6 : 1 }}>\n        <Results items={results} />\n      </div>\n    </>\n  );\n}"},{"type":"paragraph","text":"React can interrupt and restart a transition. If another keystroke arrives while the results are still rendering, the in-progress work is abandoned and the newer input wins - so the input never lags behind the user."},{"type":"heading","text":"isPending"},{"type":"paragraph","text":"The flag lets you show that something is happening without hiding the old content. Dimming or a small spinner beats replacing results with a skeleton the user has already seen."},{"type":"callout","variant":"note","text":"A transition does not make the work faster. It changes the priority, so urgent updates are not stuck behind slow ones. If a single render takes 500ms, it still takes 500ms - it just no longer blocks typing."},{"type":"heading","text":"useDeferredValue"},{"type":"paragraph","text":"When you cannot wrap the update - because the value comes from a prop, or from a library - defer the value instead. React renders with the old value first, then re-renders with the new one at a lower priority."},{"type":"code","code":"function Results({ query }) {\n  const deferredQuery = useDeferredValue(query);\n  const isStale = query !== deferredQuery;\n\n  const items = useMemo(() => search(deferredQuery), [deferredQuery]);\n\n  return (\n    <div style={{ opacity: isStale ? 0.6 : 1 }}>\n      <List items={items} />\n    </div>\n  );\n}"},{"type":"callout","variant":"tip","text":"`useTransition` when you control the state update; `useDeferredValue` when you only receive the value. Both keep old content visible instead of flashing a fallback."},{"type":"heading","text":"Transitions and Suspense together"},{"type":"paragraph","text":"An update inside `startTransition` that causes a component to suspend does **not** show the fallback. React keeps the current screen visible until the new one is ready, which avoids replacing good content with a spinner during navigation."}]},"level":{"slug":"advanced","title":"Advanced"},"previous":{"slug":"lazy-loading-components","title":"Lazy Loading Components","description":"Splitting the bundle with lazy and Suspense.","order":2,"path":"/tutorials/advanced/suspense-and-transitions/lazy-loading-components","blocks":[{"type":"paragraph","text":"`lazy` takes a function returning a dynamic import and gives you a component. The chunk is fetched the first time the component renders."},{"type":"code","code":"import { lazy, Suspense } from \"react\";\n\nconst SettingsPanel = lazy(() => import(\"./SettingsPanel\"));\n\nfunction App() {\n  const [showSettings, setShowSettings] = useState(false);\n\n  return (\n    <>\n      <button onClick={() => setShowSettings(true)}>Settings</button>\n      <Suspense fallback={<Spinner />}>\n        {showSettings && <SettingsPanel />}\n      </Suspense>\n    </>\n  );\n}"},{"type":"callout","variant":"warning","text":"Declare the `lazy` component at module level. Calling `lazy` inside a component creates a new component type on every render, which remounts the subtree and refetches the chunk."},{"type":"heading","text":"The module must have a default export"},{"type":"code","code":"// SettingsPanel.jsx\nexport default function SettingsPanel() { /* ... */ }\n\n// For a named export, map it yourself\nconst Chart = lazy(() =>\n  import(\"./charts\").then((module) => ({ default: module.BarChart }))\n);"},{"type":"heading","text":"Preloading"},{"type":"paragraph","text":"Waiting until the click to start downloading adds a visible delay. Start the fetch on hover or focus, so the chunk is usually ready by the time it is needed."},{"type":"code","code":"const load = () => import(\"./SettingsPanel\");\nconst SettingsPanel = lazy(load);\n\n<button onMouseEnter={load} onFocus={load} onClick={open}>\n  Settings\n</button>"},{"type":"heading","text":"Handling a failed chunk"},{"type":"paragraph","text":"A network failure while loading a chunk throws, so pair `Suspense` with an error boundary - covered in the next tutorial."},{"type":"code","code":"<ErrorBoundary fallback={<p>Could not load this section.</p>}>\n  <Suspense fallback={<Spinner />}>\n    <SettingsPanel />\n  </Suspense>\n</ErrorBoundary>"}]},"next":{"slug":"optimistic-updates","title":"Optimistic Updates","description":"Showing the result before the server confirms it.","order":4,"path":"/tutorials/advanced/suspense-and-transitions/optimistic-updates","blocks":[{"type":"paragraph","text":"Waiting for a round trip makes an interface feel slow even when it is fast. An **optimistic update** shows the expected result immediately and reconciles when the server responds."},{"type":"code","code":"import { useOptimistic, useState } from \"react\";\n\nfunction MessageList({ messages, onSend }) {\n  const [optimisticMessages, addOptimistic] = useOptimistic(\n    messages,\n    (current, newMessage) => [...current, { ...newMessage, isSending: true }]\n  );\n\n  async function handleSend(text) {\n    addOptimistic({ id: \"temp\", text });\n    await onSend(text);   // when this resolves, messages updates for real\n  }\n\n  return (\n    <ul>\n      {optimisticMessages.map((message) => (\n        <li key={message.id} style={{ opacity: message.isSending ? 0.5 : 1 }}>\n          {message.text}\n        </li>\n      ))}\n    </ul>\n  );\n}"},{"type":"paragraph","text":"React discards the optimistic state automatically once the underlying value updates, so there is no manual rollback to write for the success path."},{"type":"heading","text":"When optimism is appropriate"},{"type":"list","items":["The action almost always succeeds - liking, toggling, adding a comment.","Failure is easy to communicate and easy to undo.","The user would otherwise stare at a spinner for an action they expect to be instant."]},{"type":"callout","variant":"warning","text":"Do not be optimistic about payments, deletions or anything irreversible. If the operation can plausibly fail, showing success first is misleading."},{"type":"heading","text":"Handling failure"},{"type":"code","code":"async function handleSend(text) {\n  addOptimistic({ id: \"temp\", text });\n  try {\n    await onSend(text);\n  } catch (error) {\n    // The optimistic entry disappears on the next render; tell the user why\n    showToast(\"Message failed to send. Try again.\");\n  }\n}"},{"type":"heading","text":"What you have learned"},{"type":"list","items":["`Suspense` declares a fallback for a subtree; something must actually suspend for it to appear.","`lazy` splits the bundle - declare it at module level and preload on hover.","`useTransition` marks updates as interruptible so urgent ones stay instant.","`useDeferredValue` does the same when you only have the value, not the setter.","`useOptimistic` shows the expected result immediately for actions that rarely fail."]},{"type":"editor","title":"Decide whether content is stale","instructions":"Complete isStale so it returns true when the current value differs from the deferred one.","mode":"js","starterCode":"function isStale(current, deferred) {\n  // Return true when the two values differ\n}\n\nconsole.log(isStale(\"abc\", \"ab\"));","solution":"function isStale(current, deferred) {\n  return current !== deferred;\n}\n\nconsole.log(isStale(\"abc\", \"ab\"));","tests":[{"name":"Different values are stale","code":"return isStale('abc', 'ab') === true;"},{"name":"Equal values are not stale","code":"return isStale('abc', 'abc') === false;"},{"name":"Empty strings are handled","code":"return isStale('', '') === false;"}]}]},"title":"Transitions | Suspense and Transitions | React Tutorial","description":"Marking updates as non-urgent so typing never stutters. Some updates must feel instant - a keystroke appearing in an input. Others can wait a moment - the..."},"__N_SSG":true}